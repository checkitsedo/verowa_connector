plugin.tx_verowaconnector_eventlist {
    view {
        # cat=plugin.tx_verowaconnector_eventlist/file; type=string; label=Path to template root (FE)
        templateRootPath = EXT:verowa_connector/Resources/Private/Templates/
        # cat=plugin.tx_verowaconnector_eventlist/file; type=string; label=Path to template partials (FE)
        partialRootPath = EXT:verowa_connector/Resources/Private/Partials/
        # cat=plugin.tx_verowaconnector_eventlist/file; type=string; label=Path to template layouts (FE)
        layoutRootPath = EXT:verowa_connector/Resources/Private/Layouts/
    }
    persistence {
        # cat=plugin.tx_verowaconnector_eventlist//a; type=string; label=Default storage PID
        storagePid =
    }
}
